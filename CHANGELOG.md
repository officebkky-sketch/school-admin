# Changelog

ประวัติการปรับปรุงระบบบริหารจัดการข้อมูลโรงเรียน (School Admin System)

## [1.1.17] - 2026-07-31
### Added
- เพิ่มระบบ **กำหนดส่งงาน / หมดเขตดำเนินการ (Action Deadline & Smart Reminders)**:
  - สกัด `action_deadline` (วันครบกำหนดส่งงาน) จาก Gemini OCR ลงฐานข้อมูล `incoming_docs` โดยอัตโนมัติ
  - เพิ่ม Vercel Cron Job แจ้งเตือนหนังสือใกล้ครบกำหนด (`/api/deadline-reminder`) ทำงานทุกเช้า 07:00 น. ผ่าน Telegram กลุ่มส่วนกลาง
  - เพิ่มปุ่มกด Interactive Action Buttons ใน Telegram (`[✅ ดำเนินการแล้ว]` และ `[⏰ เลื่อน 3 วัน]`) ช่วยให้ครูและผู้บริหารตอบสนองอัปเดตสถานะได้ทันทีจากช่องแชท
  - เพิ่มช่องป้อนกำหนดส่งงาน (`Deadline`) บนฟอร์มลงรับหนังสือใหม่ในหน้า `IncomingDocs.tsx`
  - เพิ่มการ์ดแสดงสถิติ Real-time **"ใกล้ครบกำหนด (Deadline)"** บนหน้า `Dashboard.tsx`
- เพิ่มระบบ **สำรองข้อมูลอัตโนมัติประจำสัปดาห์ (Weekly Database Backup)**:
  - ดึงข้อมูลฐานข้อมูล 10 ตารางหลัก บีบอัดเป็นไฟล์ Gzip `.json.gz` ขนาดเล็ก
  - จัดส่งเข้า Telegram Private Chat ของผู้ดูแลระบบและผู้อำนวยการอัตโนมัติทุกเช้าวันอาทิตย์ 07:00 น. (`/api/weekly-backup`)
- เพิ่มระบบ **Global Toast Notification & Glassmorphic Modal UI**:
  - อัปเกรด `Modal.tsx` เป็น Modern Glassmorphic 3D Design (Backdrop blur, Top gradient accent, Micro-animations)
  - เพิ่มระบบ `ToastContainer.tsx` และ `toast.ts` แสดงแจ้งเตือนผลการทำงาน (Success, Error, Warning, Info) ที่มุมขวาล่างของหน้าจอ
  - เพิ่มป้ายกำกับสถานะการผูก Telegram (`✈️ ผูก Telegram แล้ว` / `⚪ ยังไม่ผูก Telegram`) บนตารางจัดการผู้ใช้ `Users.tsx`
- เพิ่มระบบ **ลืมรหัสผ่าน (Forgot Password)** สำหรับผู้ใช้งาน:
  - หน้า `ForgotPasswordView`: กรอกอีเมลเพื่อรับลิงก์รีเซ็ตรหัสผ่าน รองรับระบบ Multi-school
  - หน้า `ResetPasswordModal`: ตั้งรหัสผ่านใหม่พร้อม Password Strength Indicator, Requirements Checklist, และ Confirm Validation
  - ปุ่ม **"ลืมรหัสผ่าน?"** ในหน้า Login ใต้ช่องรหัสผ่าน

### Updated & Improved
- ปรับปรุงระบบค้นหาหนังสือบน Telegram Bot (`extractDocSearchWord` & `buildThaiDocOrFilter`):
  - สกัดคีย์เวิร์ดรองรับคำเกริ่นนำสารบรรณภาษาไทยทุกรูปแบบ เช่น *"หนังสือเลขที่รับ 415"*, *"ขอรายละเอียดหนังสือเรื่อง..."*
  - รองรับการค้นหาจากลำดับเลขที่รับของโรงเรียน (`doc_sequence`) แบบ Numeric Match Direct 100%
  - แสดงผลเลขที่รับเป็นตัวเลขอารบิก และแสดงเลขที่หนังสือต้นทางจากหน่วยงานผู้ส่งใต้เลขที่รับอย่างสวยงาม
- ปรับปรุงการส่งลิงก์แนบเอกสารบน Telegram: แทรกลิงก์ `file_url` (หนังสือนำส่ง) และ `attachment_urls` (สิ่งที่ส่งมาด้วย) กลับในคำตอบ Telegram สวยงามเป็น HTML Link `<a href="...">`
- เพิ่มระบบ **Error Alerting**: ส่งข้อความแจ้งเตือนข้อผิดพลาด OCR เข้า Telegram ของ Admin ทันทีเมื่อเกิดข้อผิดพลาดในการประมวลผลไฟล์

### Fixed
- แก้ไขปัญหา OCR เขียนทับเลขที่รับของโรงเรียน (`doc_number`) โดยย้ายเลขที่หนังสือต้นทางไปจัดเก็บใน `remark.sender_doc_number`
- แก้ไขปัญหา TypeScript Build errors บน Vercel:
  - ปรับการนำเข้า `ToastItem` ใน `ToastContainer.tsx` ให้เป็น Type-only import สอดคล้องกับ `verbatimModuleSyntax`
  - ปรับขอบเขตตัวแปร `supabase` และ `botToken` ใน `ocr-process.ts` ให้เข้าถึงได้จาก `catch` block
  - ปรับ `Uint8Array` ห่อหุ้ม `Buffer` สำหรับการสร้าง `Blob` ไฟล์ `.json.gz` ใน `weekly-backup.ts`

## [1.1.15] - 2026-07-22
### Added
- เพิ่มการสร้าง **"รายงานเด็กเข้า ป.1 (แนบ 386)"** (บัญชีรายชื่อเด็กที่มีอายุถึงเกณฑ์ภาคบังคับตามพระราชบัญญัติการศึกษาแห่งชาติ พ.ศ. 2545) ในขนาด A4 แนวนอน
- เพิ่มฟังก์ชัน **"แก้ไขข้อมูลแบบ Inline"** โดยสามารถดับเบิลคลิก/พิมพ์แก้ไขข้อมูลในตารางพิมพ์รายงาน เช่น ชั้นเรียน, ชื่อบิดา-มารดา, ผู้ปกครอง และสถานศึกษาปลายทางได้ทันทีก่อนพิมพ์ พร้อมซ่อนขอบตารางเมื่อพิมพ์ออกจริง
- เพิ่ม **"ปุ่มบันทึกข้อมูลที่แก้ไข"** ในหน้าพรีวิวก่อนสั่งพิมพ์ โดยจะปรากฏขึ้นโดยอัตโนมัติเมื่อมีการแก้ไขข้อมูลบนตาราง ช่วยบันทึกข้อมูลทุกช่อง (รวมถึงชั้นเรียนและผู้ปกครอง) ย้อนกลับไปอัปเดตลงฐานข้อมูลจริงใน Supabase ถาวรทันที
- เพิ่มคอลัมน์ใหม่ในตารางฐานข้อมูล: **`school_enrolled`** (สถานศึกษาที่เข้าเรียน), **`guardian_name`** (ชื่อผู้ปกครอง) และ **`enroll_class`** (ชั้นเรียนที่เข้าเรียน) เพื่อรองรับการเก็บข้อมูลเพิ่มเติมในระบบ ทร.14

### Updated
- ปรับปรุงการนำเข้าข้อมูล Excel (ทร.14) ทั้ง 2 รูปแบบ ให้สามารถแมปปิ้งคอลัมน์ที่ยืดหยุ่นและรองรับข้อมูลสถานศึกษาที่เข้าเรียนได้อย่างสมบูรณ์
- ปรับปรุงโครงสร้าง Sidebar และ Header รวมถึงกล่องอัปเดต ให้ซ่อนตัวโดยอัตโนมัติด้วย JavaScript DOM Manipulation และ CSS `@media print` เมื่ออยู่ในโหมดสั่งพิมพ์
- ปรับปรุงเลย์เอาต์การพิมพ์หน้ารายงานแนบ 386 ให้ย่นระยะห่างของส่วนเซ็นชื่อผู้รับรองข้อมูลให้กระชับสวยงามพอดีกับหน้ากระดาษ

### Fixed
- แก้ไขปัญหาข้อผิดพลาดการคอมไพล์ Vite / Oxc compiler (Parse Error) จาก precedence การเขียน Nullish Coalescing (`??`) ร่วมกับ Logical OR (`||`) โดยจัดระเบียบกลุ่มวงเล็บอย่างถูกต้อง
- แก้ไขปัญหาเอกสารพิมพ์ตกหล่นหรือพิมพ์ทับหน้าเมนูแอปพลิเคชัน (พิมพ์ติด Sidebar เมนู) ในเบราว์เซอร์และ Electron โดยทำการปลดล็อก parent containers สูงและ overflow ทั้งหมดในตอนพิมพ์

## [1.1.14] - 2026-06-24
### Added
- เพิ่มโมดูลย่อย **"ส่งแผนการสอน" (Lesson Plans Submission)** เพื่อช่วยให้ครูและบุคลากรทางการศึกษสามารถอัปโหลด ตรวจสอบ ติดตาม และส่งแผนการจัดการเรียนรู้ให้ฝ่ายวิชาการและผู้อำนวยการอนุมัติได้โดยตรงผ่านระบบ
- เพิ่มหน้าใบเสนออนุมัติแผนการสอนพิมพ์ขนาด A4 อิงตามระเบียบงานสารบรรณ โดยจัดตำแหน่ง "บันทึกข้อความ" กึ่งกลางหน้ากระดาษ และใช้ตราครุฑ (ขนาด 1.5 cm) ที่มุมซ้ายบนอย่างถูกต้องสวยงาม
- เพิ่มปุ่มฟังก์ชันให้ครูทั่วไปสามารถลบแผนการสอนของตนเองได้ทุกสถานะ (รวมถึงแผนที่อนุมัติแล้ว โดยจะมีหน้าต่างแจ้งเตือนยืนยันล่วงหน้า)
- เพิ่มระบบลงทะเบียนนักกีฬา (ระดับจังหวัด): แปลงตัวเลขทั้งหมดในเอกสารหนังสือรับรองให้เป็น **"ตัวเลขไทย"** โดยอัตโนมัติ (เช่น วันเกิด, ปี พ.ศ. เกิด, อายุ, ระดับชั้นเรียนและห้องเรียน เช่น ป.๖/๑)
- เพิ่มระบบลงทะเบียนนักกีฬา (ระดับจังหวัด): ทำให้ข้อมูล "ครั้งที่จัด" และ "ปีการศึกษา" ในใบหนังสือรับรองและแบนเนอร์ของระบบเปลี่ยนอัตโนมัติแบบไดนามิกอ้างอิงตามปีที่ผู้ใช้เลือก (คำนวณจาก `ปีที่เลือก - 2492` เช่น ปี ๒๕๖๙ = ครั้งที่ ๗๗)

### Updated
- ปรับปรุงการจำกัดสิทธิ์เข้าถึง (Access Control) ของหน้าระบบวิชาการ:
  - สำหรับ **ครูทั่วไป (Teacher)**: เปลี่ยนกลุ่มเมนูวิชาการใน Sidebar เป็น **"งานแผนการสอน"** และเข้าถึงได้เฉพาะส่วน **"ส่งแผนการสอน"** เท่านั้น (ซ่อนแท็บทะเบียนวิชา, มอบหมายงานสอน, และตารางเรียน/สอน)
  - สำหรับ **หัวหน้าวิชาการ / ผอ. / แอดมิน**: ยังคงเห็นกลุ่มเมนูเป็น **"งานวิชาการ"** และเข้าใช้งานฟังก์ชัน **ทะเบียนวิชา**, **มอบหมายงานสอน**, และ **ตารางเรียน/สอน** ได้ตามปกติทั้งหมด
- แยกสิทธิ์ในระบบห้องสมุด (Library) ไม่ให้ครูธรรมดาเข้าถึงได้อย่างเป็นทางการ
- ปรับปรุงการพิมพ์หนังสือรับรอง (ระดับจังหวัดและทั่วไป): บังคับใช้ขนาดฟอนต์ **16pt (TH Sarabun)** บนแท็กข้อความโดยตรง ป้องกันสไตล์ภายนอกเขียนทับ
- ปรับปรุงระยะห่างลายเซ็นและส่วนประทับตราสถานศึกษาในหนังสือรับรอง โดยลด `lineHeight` เหลือ **`1.4`** และรีเซ็ต `margin` เพื่อความเป็นระเบียบและสวยงามตามระเบียบเอกสารราชการไทย
- ปรับปรุงระบบพิมพ์เอกสาร: ซ่อนแถบตราสัญลักษณ์และเครดิตส่วนท้ายของระบบ (`IdentityFooter`) ขณะสั่งปริ้น เพื่อป้องกันปัญหาพิมพ์กระดาษเกิน/หน้าเปล่าในหน้าสุดท้าย

### Fixed
- แก้ไขปัญหาข้อผิดพลาด TypeScript ในระบบ (เช่น Property 'signature_url' Error) และการรีไดเรกต์วนซ้ำ (Redirection Loop) สำหรับแถบเมนูใน `App.tsx`
- ปรับปรุงการทำงานของโมดูล AR Learning (น้องชบาพาพิชิต) ให้รองรับการแยกข้อมูลและการเปรียบเทียบในโหมด Sequencing (เรียงลำดับขั้นตอน) และ Matching (จับคู่คำศัพท์) ในฝั่งผู้เรียนและผู้สอนได้อย่างมั่นคงขึ้น
- แก้ไขการดึงเพศจากระบบตารางหลักในโมดูลลงทะเบียนนักกีฬา (ระดับจังหวัด): แก้ไขการเช็คเพศเพื่อรองรับตัวย่อ `"ช"` และ `"ญ"` จากฐานข้อมูลนักเรียน ป้องกันการแสดงเพศผิดพลาดเป็นเพศหญิงทั้งหมด
- แก้ไขปุ่มลบรายชื่อนักกีฬาในระดับจังหวัด: อัปเดตตารางและลบออกจากสถานะแบบเลือกพิมพ์ (Print Selection) บนหน้าจอเบราว์เซอร์ทันทีหลังทำรายการสำเร็จโดยไม่ต้อง Refresh หน้าเว็บ
- แก้ไขปุ่มลงทะเบียนนักกีฬาในระดับจังหวัด: ปรับปรุงให้หน้าจอรีเฟรชอัปเดตข้อมูลนักกีฬาที่เพิ่งลงทะเบียนเสร็จใหม่เข้ามาในตารางทันทีโดยไม่ต้องทำการ Refresh หน้าเว็บ

## [1.1.13] - 2026-06-18
### Added
- เพิ่มระบบโมดูล **เด็กในเขตพื้นที่บริการ (ทร.14/พฐ.03)** สำหรับบันทึกและแก้ไขข้อมูลเด็กจากทะเบียนบ้าน ทร.14 แบบออฟไลน์/ออนไลน์
- เพิ่มการสร้างแบบรายงานบัญชีรายชื่อเด็กเข้าเรียนเกณฑ์ศึกษาภาคบังคับ (**แบบ พฐ.03**) ในขนาด A4 แนวนอน (Landscape Mode)
- เพิ่มฟังก์ชันสำหรับคัดกรองหมู่ที่ (Village Filtering) และจัดระเบียบการเรียงข้อมูลเด็กตามหมายเลขหมู่ที่ย่อยและตามชื่อจริง (ก-ฮ)
- เพิ่มช่องป้อนกำหนดเขตบริการของโรงเรียนได้เองตามต้องการผ่านหน้าจอในโหมดพรีวิวสิ่งพิมพ์
- เพิ่มระบบตัวแปลงวันเกิดภาษาไทย (Thai Date Parser) เพื่อสกัดและแปลงค่าเดือนย่อไทยกับปี พ.ศ. จาก Excel นำเข้าฐานข้อมูล Supabase ได้อย่างไร้รอยต่อ

### Updated
- อัปเดตสิทธิ์ระบบเบิกค่าสาธารณูปโภคใน [Utilities.tsx](./src/pages/Utilities.tsx) ให้รองรับการทำงานของผู้อำนวยการ (`director`) และผู้ดูแลระบบ (`admin`)
- ปรับปรุงและปิดช่องโหว่ความปลอดภัยระดับวิกฤตของ Supabase:
  - เปิดใช้งาน RLS บนตาราง `budget_allocations` ป้องกันการดัดแปลงข้อมูลการเงินจากภายนอก
  - จำกัดสิทธิ์การอ่านข้อมูลแบบ SELECT ของตาราง `profiles` (ข้อมูลครู/ลายเซ็น ผอ.) และตาราง `settings` (Gemini API Key/LINE Token) ให้เฉพาะผู้ใช้ที่ผ่านการเข้าสู่ระบบแล้วเท่านั้น (`auth.uid() IS NOT NULL`) เพื่อปิดช่องโหว่การรั่วไหลคีย์ลับสู่สาธารณะ
- อัปเดตสคริปต์ฐานข้อมูล [สคริปต์_ตารางฐานข้อมูล_Supabase.sql](./คู่มือติดตั้ง/สคริปต์_ตารางฐานข้อมูล_Supabase.sql) และเอกสาร [คู่มือการตั้งค่าระบบ.md](./คู่มือติดตั้ง/คู่มือการตั้งค่าระบบ.md) ในส่วนท้ายของคู่มือการติดตั้งเพื่อบันทึกประวัติความก้าวหน้าและการปรับปรุงด้านความปลอดภัยล่าสุด

## [1.1.12] - 2026-06-16
### Added
- เพิ่มการรองรับและสลับโมเดล **`gemini-3.1-flash-lite`** ขึ้นมาเป็นอันดับที่ 1 สำหรับการประมวลผลคำสั่งด่าน AR และฟังก์ชันสรุปทั่วไป เพื่อลดผลกระทบจาก Rate Limit (429) โดยรุ่น Lite นี้มีโควต้าการใช้งานฟรีสูงถึง 500 ครั้งต่อวัน

### Fixed
- แก้ไขรูปแบบตัวแปร `generationConfig` ใน `src/lib/aiService.ts` กลับมาเป็น **CamelCase** (`responseMimeType`, `maxOutputTokens`) เพื่อความเข้ากันได้ 100% กับโมเดลทางการของ Google REST API
- แก้ไขปัญหา Error 400 จาก Google Server โดยการปรับชื่อพารามิเตอร์จาก `body.system_instruction` เป็น **`body.systemInstruction`** (CamelCase)
- บันทึกการจำแนกสิทธิ์ผู้ใช้ในด่าน AR: ครูทั่วไปมองเห็นเฉพาะด่านของตนเอง ส่วน ผอ. และฝ่ายวิชาการมองเห็นด่านของครูทุกคนในโรงเรียนทั้งหมด
- บันทึกการแก้ไขปัญหา Supabase 400 Bad Request โดยบังคับให้ระบบสร้างรหัส ID เป็นรูปแบบ **UUID v4** ก่อนจัดเก็บข้อมูลออนไลน์ลงในฐานข้อมูล
