# Changelog

ประวัติการปรับปรุงระบบบริหารจัดการข้อมูลโรงเรียน (School Admin System)

## [1.1.14] - 2026-06-20
### Added
- เพิ่มโมดูลย่อย **"ส่งแผนการสอน" (Lesson Plans Submission)** เพื่อช่วยให้ครูและบุคลากรทางการศึกษสามารถอัปโหลด ตรวจสอบ ติดตาม และส่งแผนการจัดการเรียนรู้ให้ฝ่ายวิชาการและผู้อำนวยการอนุมัติได้โดยตรงผ่านระบบ
- เพิ่มหน้าใบเสนออนุมัติแผนการสอนพิมพ์ขนาด A4 อิงตามระเบียบงานสารบรรณ โดยจัดตำแหน่ง "บันทึกข้อความ" กึ่งกลางหน้ากระดาษ และใช้ตราครุฑ (ขนาด 1.5 cm) ที่มุมซ้ายบนอย่างถูกต้องสวยงาม
- เพิ่มปุ่มฟังก์ชันให้ครูทั่วไปสามารถลบแผนการสอนของตนเองได้ทุกสถานะ (รวมถึงแผนที่อนุมัติแล้ว โดยจะมีหน้าต่างแจ้งเตือนยืนยันล่วงหน้า)

### Updated
- ปรับปรุงการจำกัดสิทธิ์เข้าถึง (Access Control) ของหน้าระบบวิชาการ:
  - สำหรับ **ครูทั่วไป (Teacher)**: เปลี่ยนกลุ่มเมนูวิชาการใน Sidebar เป็น **"งานแผนการสอน"** และเข้าถึงได้เฉพาะส่วน **"ส่งแผนการสอน"** เท่านั้น (ซ่อนแท็บทะเบียนวิชา, มอบหมายงานสอน, และตารางเรียน/สอน)
  - สำหรับ **หัวหน้าวิชาการ / ผอ. / แอดมิน**: ยังคงเห็นกลุ่มเมนูเป็น **"งานวิชาการ"** และเข้าใช้งานฟังก์ชัน **ทะเบียนวิชา**, **มอบหมายงานสอน**, และ **ตารางเรียน/สอน** ได้ตามปกติทั้งหมด
- แยกสิทธิ์ในระบบห้องสมุด (Library) ไม่ให้ครูธรรมดาเข้าถึงได้อย่างเป็นทางการ

### Fixed
- แก้ไขปัญหาข้อผิดพลาด TypeScript ในระบบ (เช่น Property 'signature_url' Error) และการรีไดเรกต์วนซ้ำ (Redirection Loop) สำหรับแถบเมนูใน `App.tsx`
- ปรับปรุงการทำงานของโมดูล AR Learning (น้องชบาพาพิชิต) ให้รองรับการแยกข้อมูลและการเปรียบเทียบในโหมด Sequencing (เรียงลำดับขั้นตอน) และ Matching (จับคู่คำศัพท์) ในฝั่งผู้เรียนและผู้สอนได้อย่างมั่นคงขึ้น

## [1.1.13] - 2026-06-18
### Added
- เพิ่มระบบโมดูล **เด็กในเขตพื้นที่บริการ (ทร.14/พฐ.03)** สำหรับบันทึกและแก้ไขข้อมูลเด็กจากทะเบียนบ้าน ทร.14 แบบออฟไลน์/ออนไลน์
- เพิ่มการสร้างแบบรายงานบัญชีรายชื่อเด็กเข้าเรียนเกณฑ์ศึกษาภาคบังคับ (**แบบ พฐ.03**) ในขนาด A4 แนวนอน (Landscape Mode)
- เพิ่มฟังก์ชันสำหรับคัดกรองหมู่ที่ (Village Filtering) และจัดระเบียบการเรียงข้อมูลเด็กตามหมายเลขหมู่ที่ย่อยและตามชื่อจริง (ก-ฮ)
- เพิ่มช่องป้อนกำหนดเขตบริการของโรงเรียนได้เองตามต้องการผ่านหน้าจอในโหมดพรีวิวสิ่งพิมพ์
- เพิ่มระบบตัวแปลงวันเกิดภาษาไทย (Thai Date Parser) เพื่อสกัดและแปลงค่าเดือนย่อไทยกับปี พ.ศ. จาก Excel นำเข้าฐานข้อมูล Supabase ได้อย่างไร้รอยต่อ

### Updated
- อัปเดตสิทธิ์ระบบเบิกค่าสาธารณูปโภคใน [Utilities.tsx](./src/pages/Utilities.tsx) ให้รองรับการทำงานของผู้อำนวยการ (`director`) และผู้ดูแลระบบ (`admin`)
- ปรับปรุงและปิดช่องโหว่ความปลอดภัยระดับวิกฤตของ Supabase:
  - เปิดใช้งาน RLS บนตาราง `budget_allocations` ป้องกันการดัดแปลงข้อมูลการเงินจากภายนอก
  - จำกัดสิทธิ์การอ่านข้อมูลแบบ SELECT ของตาราง `profiles` (ข้อมูลครู/ลายเซ็น ผอ.) และตาราง `settings` (Gemini API Key/LINE Token) ให้เฉพาะผู้ใช้ที่ผ่านการเข้าสู่ระบบแล้วเท่านั้น (`auth.uid() IS NOT NULL`) เพื่อปิดช่องโหว่การรั่วไหลคีย์ลับสู่สาธารณะ
- อัปเดตสคริปต์ฐานข้อมูล [สคริปต์_ตารางฐานข้อมูล_Supabase.sql](./คู่มือติดตั้ง/สคริปต์_ตารางฐานข้อมูล_Supabase.sql) และเอกสาร [คู่มือการตั้งค่าระบบ.md](./คู่มือติดตั้ง/คู่มือการตั้งค่าระบบ.md) ในส่วนท้ายของคู่มือการติดตั้งเพื่อบันทึกประวัติความก้าวหน้าและการปรับปรุงด้านความปลอดภัยล่าสุด

## [1.1.12] - 2026-06-16
### Added
- เพิ่มการรองรับและสลับโมเดล **`gemini-3.1-flash-lite`** ขึ้นมาเป็นอันดับที่ 1 สำหรับการประมวลผลคำสั่งด่าน AR และฟังก์ชันสรุปทั่วไป เพื่อลดผลกระทบจาก Rate Limit (429) โดยรุ่น Lite นี้มีโควต้าการใช้งานฟรีสูงถึง 500 ครั้งต่อวัน

### Fixed
- แก้ไขรูปแบบตัวแปร `generationConfig` ใน `src/lib/aiService.ts` กลับมาเป็น **CamelCase** (`responseMimeType`, `maxOutputTokens`) เพื่อความเข้ากันได้ 100% กับโมเดลทางการของ Google REST API
- แก้ไขปัญหา Error 400 จาก Google Server โดยการปรับชื่อพารามิเตอร์จาก `body.system_instruction` เป็น **`body.systemInstruction`** (CamelCase)
- บันทึกการจำแนกสิทธิ์ผู้ใช้ในด่าน AR: ครูทั่วไปมองเห็นเฉพาะด่านของตนเอง ส่วน ผอ. และฝ่ายวิชาการมองเห็นด่านของครูทุกคนในโรงเรียนทั้งหมด
- บันทึกการแก้ไขปัญหา Supabase 400 Bad Request โดยบังคับให้ระบบสร้างรหัส ID เป็นรูปแบบ **UUID v4** ก่อนจัดเก็บข้อมูลออนไลน์ลงในฐานข้อมูล
