# 🗺️ แผนการพัฒนาระบบ AI น้องชบาอัจฉริยะ (Chaba Bot Next-Gen Roadmap)
**เป้าหมาย:** พัฒนาความสามารถในการวิเคราะห์บทสนทนา 100%, ปราศจากข้อมูลมโน (Zero Hallucination), และการจดจำพฤติกรรมประวัติการคุยรายบุคคล

---

## 🚀 1. ความเป็นไปได้เชิงเทคนิค (Feasibility)
**สามารถทำได้อย่างแน่นอน 100% ครับคุณครู!** ด้วยสถาปัตยกรรมปัจจุบันที่เราใช้ (Supabase + Vercel + Gemini API) ตัวระบบมีเครื่องมือและทรัพยากรที่พร้อมรับช่วงต่อเพื่อพัฒนาความสามารถนี้ได้ทันที โดยแบ่งการดำเนินการออกเป็น 3 ระยะหลักดังนี้ครับ:

---

## 📅 2. แผนการดำเนินงาน (Implementation Steps)

### 🎯 ระยะที่ 1: คุมเข้มข้อมูลจริง 100% (Zero Hallucination)
*เน้นป้องกันไม่ให้ AI สร้างข้อมูลสมมติขึ้นมาเองเด็ดขาด*
1. **การปรับแต่ง Strict System Prompt:** ปรับปรุง System Instruction ของ Gemini ให้ใช้เงื่อนไขตรวจสอบบังคับ (Constraint Enforcement) เช่น กำหนดให้ใช้เฉพาะข้อมูลที่อยู่ในแท็ก JSON เท่านั้น หากพบค่าน้อยเกินไป ให้ตอบปฏิเสธด้วยประโยคสำเร็จรูปแทนการพยายามเดา
2. **การติดตั้ง Fallback UI:** หากผลการคิวรีฐานข้อมูลส่งกลับมาเป็นค่าว่าง บอทจะสกัดไม่ส่งข้อมูลไปให้ Gemini และเลือกแสดงข้อความชี้แจงสำเร็จรูปพร้อมปุ่มแนะนำ (เช่น *"ขออภัยค่ะ ไม่พบข้อมูลหนังสือรับนี้ในระบบ คุณครูต้องการให้ค้นหาเอกสารเรื่องอื่นแทนไหมคะ?"*) เพื่อจำกัดความผิดพลาดเป็น 0%

### 🔍 ระยะที่ 2: ระบบค้นหาผ่านความหมาย (Semantic & Vector Search)
*แก้ปัญหาบอทเงียบหรือหาข้อมูลไม่เจอเนื่องจากคำสะกดไม่ตรง หรือเปลี่ยนคำถามในภาษาพูด*
1. **เปิดใช้งาน `pgvector` ใน Supabase:** ติดตั้งส่วนขยาย pgvector ในฐานข้อมูลของโรงเรียนเพื่อเก็บเวกเตอร์ของคำ (Embeddings)
2. **ระบบแปลงข้อมูลเป็น Vector:** เมื่อมีการลงรับหนังสือรับ หรืออัปเดตข้อมูลโครงการ ระบบจะนำชื่อเรื่องแปลงเป็น Vector (ผ่าน Gemini Embedding API) และบันทึกลงในคอลัมน์ `embedding`
3. **การเปรียบเทียบหาความหมายใกล้เคียง:** เมื่อคุณครูทักถามมาในแชท ระบบจะนำคำถามของคุณครูไปเปรียบเทียบระยะห่างทางเวกเตอร์ (Cosine Similarity) ในฐานข้อมูล เพื่อดึงหนังสือรับที่มีเนื้อหาใกล้เคียงที่สุดมาตอบแทนการจับคำตรงๆ (Exact Match) แบบเดิม

### 🧠 ระยะที่ 3: ระบบจดจำประวัติและพฤติกรรมรายบุคคล (Personalized Chat Memory)
*ทำให้บอทรู้ว่ากำลังคุยกับใคร จำพฤติกรรมความชอบ และงานที่กำลังทำอยู่ของครูคนนั้นได้*
1. **สร้างตาราง `chat_memories` ใน Supabase:**
   ```sql
   CREATE TABLE chat_memories (
     id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
     telegram_chat_id TEXT NOT NULL,
     user_id UUID REFERENCES profiles(id) ON DELETE CASCADE,
     summary_memory JSONB DEFAULT '{}', -- เก็บความจำสรุปพฤติกรรม
     chat_history JSONB DEFAULT '[]',   -- เก็บประวัติการคุยล่าสุด 5-10 ข้อความ
     updated_at TIMESTAMPTZ DEFAULT NOW()
   );
   ```
2. **กลไกการทำงานเพื่อตอบโจทย์พฤติกรรมเฉพาะคน:**
   * **การดึงความจำ:** เมื่อครูทักบอทมา บอทจะดึง `summary_memory` และประวัติการคุยจากฐานข้อมูลขึ้นมาเป็นบริบทเพิ่มเติม
   * **ตัวอย่างการปรับแต่งการตอบ:** 
     * หากบอทจำได้ว่าคุณครูท่านนี้มักทำงานการเงิน บอทจะเน้นการจัดเรียงรายงานการซื้อจ้างให้เป็นอันดับแรกโดยไม่ต้องร้องขอเพิ่ม
     * หากบอทจำได้ว่าคุณครูถามค้างเรื่องการเกษียณหนังสือเลขที่ 383 ไว้ก่อนหน้านี้ เมื่อคุณครูพิมพ์ว่า *"สรุปเป็นยังไงบ้าง"* บอทจะตอบความคืบหน้าของเรื่อง 383 ให้ทันทีโดยที่คุณครูไม่ต้องพิมพ์เลขหนังสือซ้ำอีกครั้ง!
   * **การอัปเดตความจำ:** ทุกครั้งที่จบบทสนทนา ระบบจะสั่งให้ Gemini สรุปความต้องการใหม่ของผู้ใช้สั้นๆ แล้วนำไปเขียนทับลงฟิลด์ `summary_memory` เพื่อใช้เป็นความจำในครั้งต่อไป

---

*แผนงานนี้จัดทำขึ้นเพื่อให้คุณครูสามารถนำไปใช้อ้างอิงสำหรับการพัฒนาต่อยอดระบบ AI น้องชบาให้มีความฉลาดก้าวหน้ายิ่งขึ้นในอนาคตครับ* 🌸🤖✨
